<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      No `package` attribute: AGP 8+ takes the namespace from build.gradle.kts
      and fails the build if it is declared here as well.

      The INTERNET permission and the `RobokassaActivity` declaration come from
      Robokassa's own library manifest, which the consumer supplies (see the
      README's installation section) and which the manifest merger folds in.
    -->
</manifest>
